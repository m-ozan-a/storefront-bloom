export { catalog } from "./catalog";
export type { StorefrontCatalog } from "./catalog";
// registry/components are "use client" — import { registry } from "./components"
// or { StorefrontRenderer } from "./storefront-renderer" directly where needed.
